# 실제 값은 ansible/secrets.yml 에 작성한다 (gitignore 대상, 저장소에 올리지 않는다).
#   cp secrets.example.yml secrets.yml
# 실행 시 -e @secrets.yml 로 전달한다.
mysql_root_password: "CHANGE_ME"
order_db_user: "CHANGE_ME"
order_db_password: "CHANGE_ME"
user_db_user: "CHANGE_ME"
user_db_password: "CHANGE_ME"
